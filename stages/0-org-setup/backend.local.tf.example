# First-time bootstrap: use local backend before state bucket exists
#
# 1. Copy this file to backend_override.tf
# 2. Run terraform apply in stage 0
# 3. Remove backend_override.tf and run terraform init -migrate-state

terraform {
  backend "local" {
    path = "terraform.tfstate"
  }
}
